<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">Navbar</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarColor01"
    aria-controls="navbarColor01"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item ">
        <a class="nav-link" href="/user/managerdashboard">Home </a>
      </li>
      <li class="nav-item ">
        <a class="nav-link" href="/user/createevent">Create Event </a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="#"
          >Create Hall<span class="sr-only">(current)</span></a
        >
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/user/logout" style="margin-left: 300px;"
          >Logout</a
        >
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="text" placeholder="Search" />
      <button class="btn btn-secondary my-2 my-sm-0" type="submit">
        Search
      </button>
    </form>
  </div>
</nav>
<div class="row mt-5">
  <div class="col-md-6 m-auto">
    <div class="card card-body">
      <h1 class="text-center mb-3">
        <i class="fas fa-event-plus"></i> Create Hall
      </h1>
      <% include ./partials/messages %>
      <form action="/user/createhall" method="POST">
        <div class="form-group">
          <label for="num_rows">Rows</label>
          <input
            type="number"
            id="num_rows"
            name="num_rows"
            class="form-control"
            placeholder="Number of Rows..."
            value="<%= typeof num_rows != 'undefined' ? num_rows : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="num_seats_per_row">Seats Per Row</label>
          <input
            type="number"
            id="num_seats_per_row"
            name="num_seats_per_row"
            class="form-control"
            placeholder="Number of Seats per Row..."
            value="<%= typeof num_seats_per_row != 'undefined' ? num_seats_per_row : '' %>"
          />
        </div>
        <div class="form-group">
          <label for="hall_no">Hall Number</label>
          <input
            type="number"
            id="hall_no"
            name="hall_no"
            class="form-control"
            placeholder="Hall Number..."
            value="<%= typeof hall_no != 'undefined' ? hall_no : '' %>"
          />
        </div>
        <button type="submit" class="btn btn-primary btn-block">
          Create
        </button>
      </form>
    </div>
  </div>
</div>
