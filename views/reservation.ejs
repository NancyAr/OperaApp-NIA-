<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
  <a class="navbar-brand" href="#">EVENTS</a>
  <button
    class="navbar-toggler"
    type="button"
    data-toggle="collapse"
    data-target="#navbarColor01"
    aria-controls="navbarColor01"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarColor01">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <a class="nav-link" href="/user/dashboard">Home </a>
      </li>
      <li class="nav-item ">
        <a class="nav-link" href="/user/showevents">Events</a>
      </li>
      <li class="nav-item active">
        <a class="nav-link" href="#"
          >Reservations <span class="sr-only">(current)</span></a
        >
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/user/logout" style="margin-left: 400px;"
          >Logout</a
        >
      </li>
    </ul>
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="text" placeholder="Search" />
      <button class="btn btn-secondary my-2 my-sm-0" type="submit">
        Search
      </button>
    </form>
  </div>
</nav>

<div id="myTabContent" class="tab-content">
  <div class="tab-pane fade show active" id="home">
    <div class="jumbotron">
      <table class="table table-hover">
        <thead>
          <tr class="table-primary">
            <th scope="col">Reservations</th>
            <th scope="col"></th>
            <th scope="col"></th>
            <th scope="col"></th>
            <th scope="col"></th>

            <th scope="col"></th>
          </tr>
        </thead>
        <% include ./partials/messages %>
        <tbody>
          <% for(let i=0; i< books.length; ++i){ %>

          <tr>
            <th scope="row" id="<%= books[i].event_name %>">
              <%= books[i].event_name %>
            </th>
            <td><%= books[i].seats %></td>
            <td><%= books[i].event_date.toDateString() %></td>
            <td></td>

            <td>
              <a
                href="/user/deletereservation/<%=books[i]._id%>"
                class="btn btn-danger"
                >X</a
              >
            </td>

            <td></td>
          </tr>
          <% }%>
        </tbody>
      </table>
    </div>
  </div>
</div>
